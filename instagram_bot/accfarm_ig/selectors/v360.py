"""IG v360.x.x specific selectors."""

from accfarm_ig.selectors.registry import IGSelectors

# v360 uses the default selectors defined in registry.py
# This file exists for explicit reference and future modifications

v360_selectors = IGSelectors(
    # All defaults from IGSelectors apply
    # Override any that differ in v360
)
